# ======================================================
# 今日校园查寝签到工具 - 配置
# ======================================================
schoolName: 示例大学

# 校区坐标（高德/百度地图获取）—— 占位值，请填入你自己校区的经纬度
campuses:
  默认校区:
    lon: '0.0'
    lat: '0.0'

# 默认校区
defaultCampus: 默认校区

# 加密密钥（旧协议路径使用；新版协议已改用动态密钥，这两项仅为兼容保留）
desKey: 'XCE927=='
aesKey: 'abcdfe0987612345'

# Cookie持久化文件路径
cookieFile: '.session_cookies.json'